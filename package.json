{
  "name": "miniflare-kv-langparser",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --local --env local",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "@cloudflare/workers-types": "^4.20241022.0",
    "@types/accept-language-parser": "^1.5.6",
    "miniflare": "^3.20241022.0",
    "typescript": "^5.5.2",
    "vitest": "2.0.5",
    "wrangler": "^3.84.1"
  },
  "dependencies": {
    "accept-language-parser": "^1.5.0",
    "mimes": "^1.0.0"
  }
}
